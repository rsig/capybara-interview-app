require 'spec_helper'

describe 'Sharefile', type: :feature do
  it 'allows you to get a request link' do
    # visit sharefile url
    # login
    # Request a link
    # visit the link
    # upload a document on the link path

    # execute_script "document.getElementsByTagName('input')[0].style.visibility = 'visible'"
    # execute_script "document.getElementsByTagName('input')[0].style.height = 'initial'"
    # execute_script "document.getElementsByTagName('input')[0].style.width = 'initial'"
    # file_name = blahblah
    # find('input[type=file]').set "//Users/saravanank/Desktop/#{file_name}"

    # check whether the uploaded document is present on your folder
  end
end
